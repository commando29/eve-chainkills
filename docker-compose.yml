version: "3.8"
services:
  eve-chainkills:
    build: .
    volumes: 
      - .:/app
    container_name: eve-chainkills
    networks:
      - default
      - web
networks:
  default:
    external:
      name: pathfinder-containers_pf
  web: 
    external: true
